interface:
  display_name: "Release Review"
  short_description: "Apply a release-readiness governance lens on top of review-gate"
  default_prompt: "Review this change as a release-readiness governance overlay on top of review-gate. Focus on versioning, changelog accuracy, rollout safety, upgrade impact, and release-note honesty, then append findings to REVIEW.jsonl with a gate result."
